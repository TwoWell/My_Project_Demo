server:
  port: 9004
spring:
  application:
    name: tensquare-article
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tensquare_article?characterEncoding=utf-8&useSSL=true
    username: root
    password: mysql
  jpa:
    database: mysql
    show-sql: true
  redis:
    host: localhost